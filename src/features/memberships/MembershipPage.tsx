import { CheckCircle2, Crown, RefreshCw } from 'lucide-react';
import { useEffect, useState } from 'react';
import { formatCommerceMoney } from '../../services/commerce.service';
import { getMembershipErrorMessage, membershipService, type MembershipCatalogItem, type MembershipCheckout } from '../../services/membership.service';

export function MembershipPage() {
  const [items, setItems] = useState<MembershipCatalogItem[]>([]);
  const [current, setCurrent] = useState<Awaited<ReturnType<typeof membershipService.current>>>(null);
  const [selected, setSelected] = useState<Record<string, string>>({});
  const [confirmed, setConfirmed] = useState<Record<string, boolean>>({});
  const [changeKind, setChangeKind] = useState<Record<string, 'UPGRADE' | 'DOWNGRADE'>>({});
  const [pending, setPending] = useState<string | null>(null);
  const [checkout, setCheckout] = useState<MembershipCheckout | null>(null);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => { void Promise.all([membershipService.catalog(), membershipService.current()]).then(([catalog, active]) => { setItems(catalog.items); setCurrent(active); }).catch((reason) => setError(getMembershipErrorMessage(reason))); }, []);

  async function beginCheckout(item: MembershipCatalogItem) {
    const durationOptionId = selected[item.id] ?? item.durations[0]?.id;
    if (!durationOptionId || !confirmed[item.id]) { setError('Hãy xác nhận quyền lợi thay đổi trước khi tiếp tục.'); return; }
    setPending(item.id); setError(null);
    try { setCheckout(await membershipService.checkout({ versionId: item.id, durationOptionId, requestedChange: current && current.plan.id !== item.plan.id ? changeKind[item.id] : undefined, changedBenefitsConfirmed: true })); }
    catch (reason) { setError(getMembershipErrorMessage(reason)); }
    finally { setPending(null); }
  }

  if (checkout) return <main className="container mx-auto max-w-2xl px-4 py-12" role="status"><CheckCircle2 className="mb-4 text-primary" /><h1 className="text-2xl font-semibold">Đơn gói thành viên đã được tạo</h1><p className="mt-2">{checkout.order.orderNumber} · {formatCommerceMoney(checkout.order.payable)}</p><p className="mt-2">{checkout.paymentRequired ? 'Đơn đang chờ thanh toán; quyền lợi chỉ được kích hoạt sau khi thanh toán được máy chủ xác minh.' : 'Máy chủ sẽ xử lý đơn không cần thanh toán.'}</p></main>;
  return <main className="container mx-auto max-w-6xl px-4 py-10"><header className="mb-8"><p className="text-sm text-muted-foreground">Thành viên EduAI</p><h1 className="text-3xl font-semibold">Chọn gói phù hợp với hành trình học của bạn</h1>{current ? <p className="mt-2">Gói hiện tại: <strong>{current.displayName}</strong>, hết hạn {new Intl.DateTimeFormat('vi-VN').format(new Date(current.expiresAt))}.</p> : null}</header>{error ? <p className="mb-4 rounded border border-destructive p-3" role="alert">{error}</p> : null}<section className="grid gap-5 md:grid-cols-2" aria-label="Các gói thành viên">{items.map((item) => { const duration = item.durations.find((option) => option.id === (selected[item.id] ?? item.durations[0]?.id)); const changingPlan = current && current.plan.id !== item.plan.id; return <article className="rounded-lg border bg-card p-5" key={item.id}><Crown aria-hidden="true" className="mb-3 text-primary" /><h2 className="text-xl font-semibold">{item.displayName}</h2>{item.description ? <p className="mt-2 text-muted-foreground">{item.description}</p> : null}<ul className="mt-4 list-disc space-y-1 pl-5">{item.services.map((service) => <li key={service.code}>{service.displayName}{service.quota ? `: ${service.quota} ${service.unitLabel ?? ''}` : ''}</li>)}{item.includedCourses.map((course) => <li key={course.id}>{course.title}</li>)}</ul><label className="mt-5 block text-sm font-medium">Thời hạn<select className="mt-1 w-full rounded border p-2" onChange={(event) => setSelected((value) => ({ ...value, [item.id]: event.target.value }))} value={selected[item.id] ?? item.durations[0]?.id}>{item.durations.map((option) => <option key={option.id} value={option.id}>{option.months} tháng · {formatCommerceMoney({ amountMinor: option.finalPriceAmountMinor, currency: item.currency })}{option.discountPercent !== null ? ` (giảm ${option.discountPercent}%)` : ''}</option>)}</select></label>{changingPlan ? <label className="mt-4 block text-sm font-medium">Loại thay đổi<select className="mt-1 w-full rounded border p-2" onChange={(event) => setChangeKind((value) => ({ ...value, [item.id]: event.target.value as 'UPGRADE' | 'DOWNGRADE' }))} value={changeKind[item.id] ?? ''}><option disabled value="">Chọn nâng cấp hoặc hạ cấp</option><option value="UPGRADE">Nâng cấp — có hiệu lực sau thanh toán</option><option value="DOWNGRADE">Hạ cấp — có hiệu lực khi gói hiện tại hết hạn</option></select></label> : null}{duration ? <p className="mt-2 text-sm">Giá gốc {formatCommerceMoney({ amountMinor: duration.basePriceAmountMinor, currency: item.currency })}; giá thanh toán <strong>{formatCommerceMoney({ amountMinor: duration.finalPriceAmountMinor, currency: item.currency })}</strong>.</p> : null}<label className="mt-4 flex gap-2 text-sm"><input checked={Boolean(confirmed[item.id])} onChange={(event) => setConfirmed((value) => ({ ...value, [item.id]: event.target.checked }))} type="checkbox" />Tôi đã xem và xác nhận quyền lợi của gói này. Không có gia hạn tự động.</label><button className="mt-5 inline-flex items-center gap-2 rounded bg-primary px-4 py-2 text-primary-foreground disabled:opacity-60" disabled={pending === item.id || Boolean(changingPlan && !changeKind[item.id])} onClick={() => void beginCheckout(item)} type="button"><RefreshCw aria-hidden="true" className={pending === item.id ? 'animate-spin' : ''} />{pending === item.id ? 'Đang tạo đơn…' : 'Tiếp tục thanh toán'}</button></article>; })}</section></main>;
}
